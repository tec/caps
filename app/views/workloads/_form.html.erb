<%= form_for(@workload) do |f| %>
  <% if @workload.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workload.errors.count, "error") %> prohibited this workload from being saved:</h2>

      <ul>
      <% @workload.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
